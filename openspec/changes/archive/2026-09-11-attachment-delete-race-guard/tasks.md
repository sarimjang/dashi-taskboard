## 1. 驗證測試注入機制的可行性（對應設計決策「回歸測試：用 Miniflare service binding 作為確定性的 race 注入點，需要 apply 階段先做可行性驗證」）

- [x] [P] 1.1 在 `test/helpers/cloud-worker-harness.mjs` 使用的 Miniflare 設定裡，用一個最小驗證案例確認 `serviceBindings` 的 function-handler binding 能在 worker `fetch` 處理過程中被呼叫，且呼叫端的 Node 測試程式碼能與其同步交握（worker 端 `await` 這個 binding 的 `.fetch()` 直到 Node 端的 handler resolve 才繼續往下執行）；驗證方式：撰寫一個臨時驗證測試，worker 端在處理請求時呼叫一次這個 binding 並在 handler resolve 前後各記錄一個時間戳，斷言 handler 確實在 worker 恢復執行前被呼叫、且順序符合預期。若驗證失敗或行為與預期不符，在本任務的完成說明中明確記錄「改採 white-box SQL 測試備選方案」，後續任務 3.3／4.x 依此調整範圍。

## 2. Schema migration：新增附件變更計數器與自動遞增 trigger（對應設計決策「用 schema-level AFTER INSERT/DELETE trigger 遞增計數器，不是在每個 JS mutation 呼叫點手動加 UPDATE 陳述式」）

- [x] 2.1 新增 `cloud/migrations/0012_attachment_revision_counters.sql`（開工前先確認 `0012` 是否仍是下一個可用編號），內容包含 `projects.attachment_revision`／`tasks.attachment_revision` 兩個 `INTEGER NOT NULL DEFAULT 0` 欄位，以及四個 `AFTER INSERT`/`AFTER DELETE` trigger（`project_readme_attachments_bump_revision_insert`/`_delete` 掛在 `project_readme_attachments`；`attachments_bump_revision_insert`/`_delete` 掛在 `attachments`）；驗證方式：在 `test/helpers/cloud-worker-harness.mjs` 使用的 miniflare D1 綁定上套用這個 migration 檔案不報錯，並用一個臨時查詢對 `sqlite_master` 確認出現對應的 4 個 trigger 名稱與 2 個新欄位。
- [x] 2.2 驗證「Attachment mutations automatically advance the owning resource's attachment revision counter」需求在 SQL 層本身成立：新增一個直接對 miniflare D1 綁定操作的驗證測試，分別對 `project_readme_attachments`/`attachments` 表各執行一次 INSERT 與一次 DELETE，斷言對應 `projects`/`tasks` 列的 `attachment_revision` 各自 +1；同一個測試案例內，額外對一個有附件的 project/task 直接執行 `DELETE FROM projects`/`DELETE FROM tasks`（cascade 刪除其附件），斷言操作成功不拋出例外；驗證方式：`npm run test:cloud` 內新增的這個測試通過。
- [x] 2.3 驗證設計決策「回滾方案」中描述的完整回退路徑可行：撰寫一個回滾用的 forward migration 草稿（`DROP TRIGGER` x4 本 change 新增的四個 trigger + `ALTER TABLE projects DROP COLUMN attachment_revision` + `ALTER TABLE tasks DROP COLUMN attachment_revision`，比照 `cloud/migrations/0009_remove_workflow_schema.sql` 的既有寫法），在 miniflare D1 綁定上依序套用「0012 migration → 這個回滾草稿」，確認兩者都不報錯；驗證方式：新增的臨時驗證測試通過，且套用回滾草稿後查詢 `sqlite_master`／`pragma table_info`，確認 4 個 trigger 與 2 個欄位都已移除。這個草稿本身不需要併入 `cloud/migrations/` 目錄（回滾方案是文件化的應變手段，不是本次要上線的變更），驗證通過後可捨棄。

## 3. `deleteProject()`/`deleteArchivedTask()` guard 改寫（對應設計決策「`deleteProject()`/`deleteArchivedTask()` 讀取 revisionAtStart 的方式不同，源自既有程式碼結構的差異」與「guard SQL 改寫：比對計數器版本，維持既有錯誤碼與 API 形狀」）

- [x] 3.1 在 `work/50m`（commit `783befa`）的基礎上修改 `deleteProject()`：於呼叫 `collectProjectReadmeAttachmentIds()` 之前，新增一次 `SELECT attachment_revision FROM projects WHERE id = ?` 讀取 `revisionAtStart`，並把刪除陳述式的 guard 子查詢從 `(SELECT COUNT(*) FROM project_readme_attachments WHERE project_id = ?) = ?` 改成 `(SELECT attachment_revision FROM projects WHERE id = ?) = ?`（bind `revisionAtStart`），滿足 Requirement: Attachment deletion guard detects any change to the attachment set, not just a count match；驗證方式：`npm run test:cloud` 裡既有的 "deleting an empty project also cleans up its R2 README attachment" 測試與 50m 併入後的跨分頁測試（"deleteProject() pages past a single README-attachment-id batch..."）維持通過，行為與修改前一致（`PROJECT_ATTACHMENTS_CHANGED` 錯誤碼與訊息不變）。
- [x] 3.2 同理修改 `deleteArchivedTask()`：直接複用既有 `requireTaskRow()` 呼叫回傳的 `current.attachment_revision` 作為 `revisionAtStart`（不需要新查詢，因為 `requireTaskRow()` 是 `SELECT *`），guard 子查詢改為比對 `(SELECT attachment_revision FROM tasks WHERE id = ?) = ?`；驗證方式：既有 "permanent task deletion requires archiving and cleans D1 and R2" 測試與 50m 併入後的跨分頁測試（"deleteArchivedTask() pages past a single attachment-id batch..."）維持通過，`TASK_ATTACHMENTS_CHANGED` 錯誤碼與訊息不變。
- [x] 3.3 新增兩個回歸測試（`deleteProject()`/`deleteArchivedTask()` 各一），驗證 Requirement: Attachment deletion guard detects any change to the attachment set, not just a count match 中「一增一減淨值不變」的情境：分頁收集完成後，於窗口內刪除一個既有附件並上傳一個全新附件（若任務 1.1 驗證 `RACE_TEST_HOOK` 可行，透過它確定性注入；否則依任務 1.1 記錄的備選方案改用直接操作 `cloud.db` 的 white-box SQL 測試，並在測試檔案裡註明這是已知較弱的防護，因為它不透過真實呼叫路徑驗證）；驗證方式：兩個新測試斷言回應狀態碼為 409（`PROJECT_ATTACHMENTS_CHANGED`/`TASK_ATTACHMENTS_CHANGED`），且新上傳附件的 D1 metadata 列與 R2 物件在斷言時都還存在。

## 4. `RACE_TEST_HOOK` 測試注入點佈線（若任務 1.1 判定可行；對應設計決策「回歸測試：用 Miniflare service binding 作為確定性的 race 注入點」）

- [x] [P] 4.1 在 `cloud/src/index.mjs` 的 `deleteProject()`/`deleteArchivedTask()` 內，分頁收集完成之後、guard 刪除陳述式執行之前，各自加入一行 `if (env.RACE_TEST_HOOK) { await env.RACE_TEST_HOOK.fetch("http://race-test-hook/after-collect"); }`；驗證方式：確認 `wrangler.jsonc` 未宣告 `RACE_TEST_HOOK`（`rg "RACE_TEST_HOOK" wrangler.jsonc` 為 0 筆結果），且在未設定這個 binding 的一般測試/正式環境下，既有全套 `npm test`/`npm run test:cloud` 行為與新增這行之前完全一致（這行在沒有此 binding 時恆為 no-op）。
- [x] [P] 4.2 在 `test/helpers/cloud-worker-harness.mjs` 新增一個可選的 `serviceBindings` 設定項，讓呼叫端能在建立測試 harness 時傳入 `RACE_TEST_HOOK` 對應的 function handler；驗證方式：任務 3.3 新增的 race 回歸測試能透過這個新增的設定選項成功注入併發 mutation 並通過。

## 5. Fix-then-Audit 全 repo 掃描與既有測試迴歸確認

- [x] 5.1 全 repo `grep`/`rg` `INSERT INTO attachments|DELETE FROM attachments|INSERT INTO project_readme_attachments|DELETE FROM project_readme_attachments` 確認 `uploadAttachment()`/`uploadProjectReadmeAttachment()`/`deleteAttachment()`/`deleteComment()` 四個既有呼叫點的程式碼未被本 change 修改（trigger 設計下不需要程式碼變動）；並新增測試分別透過 `POST /api/tasks/:id/attachments`、`POST /api/projects/:id/readme/attachments`、`DELETE /api/attachments/:id`、`DELETE /api/comments/:id`（該 comment 需先帶有附件，驗證 CASCADE 間接路徑）四個 HTTP 端點各自觸發一次附件 mutation，滿足 Requirement: Attachment mutations automatically advance the owning resource's attachment revision counter；驗證方式：`git diff` 確認這四個函式在 `cloud/src/index.mjs` 裡的程式碼無變化；新增的四個測試各自透過 `cloud.db` 直接查詢，斷言對應 project/task 的 `attachment_revision` 相較呼叫前有移動。
- [x] 5.2 執行完整 `npm test` 與 `npm run test:cloud`，確認全部既有測試（含 50m 併入分頁測試、本 change 新增的全部測試）皆為綠燈，無新增失敗或既有行為變化；驗證方式：兩個指令的執行結果皆為全數通過，無需排除任何新增的失敗案例。
